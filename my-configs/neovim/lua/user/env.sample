local M = {}

-- where swap files are going to be stored
M.tempdir = false

-- path to a vim script, probably ~/my-configs/vim/theme.vim
M.theme_tweaks = false

-- make sure the cursor keeps the "beam" shape
M.preserve_beam_cursor = false

return M
